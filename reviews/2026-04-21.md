# Review — 2026-04-21

**Window:** last 24h · **Files changed:** 43

---

## Code changes

### `agent.py`
Well-structured agent loop with multi-agent support (`agent_type`: sales / marketing / None). Notable additions since the last reviewed baseline:
- `AGENT_SYSTEM_PROMPTS` dispatch imported from `prompts.py` — clean separation of concerns
- `max_iterations=15` guard with a graceful nudge message — good, prevents runaway loops
- Timing diagnostics (`_api_t0`, `_dt`) on every iteration and tool call — useful for production debugging
- **Risk:** `_time` is imported inside the loop body on every iteration. Move to top-level import for cleanliness. Not a runtime bug, but wasteful and easy to miss in a code review.
- **Risk:** The `max_iterations` fallback makes a second API call without tools, but does not append the fallback response to `messages` before returning. This means the conversation history is missing the final assistant turn if we hit the cap. Next user message would re-send everything without the cap answer, which could confuse the model. Worth verifying or fixing.

### `tools.py`
Solid file-reading layer. Changes:
- `_READ_CACHE` with FIFO eviction — good, prevents memory leak across long agent sessions
- PPTX support added (via `python-pptx`) — matches `requirements.txt`
- `web_search` via DuckDuckGo with explicit error logging to stderr — good pattern
- `search_files` caps results at 20 — reasonable
- **Risk:** `search_files` reads every file in the source layer on every call (full scan). With 20+ expert profiles and proposal docs this will get slow as the corpus grows. No index or short-circuit. Not urgent now, but flag for when corpus exceeds ~200 files.
- **Cleared:** Content-type dispatch (`docx`, `pdf`, `xlsx`, `pptx`) is correct and has proper exception wrapping.

### `feedback.py`
Append-only feedback loop. Clean and correct:
- Thread-safe via `Lock` — correct
- `_format_blockquote` handles multi-line comments
- Header written once on first entry, checked by file size
- **Minor:** `from pathlib import Path as _Path` is imported twice (lines 4 and 20). Duplicate import — harmless but should be removed.

### `memory.py`
SQLite-backed conversation history. Robust:
- Schema version guard (refuses newer-on-disk schema) — good protection against running old code against migrated DB
- `_to_jsonable` handles pydantic SDK content blocks via `.model_dump()` — this was the fix for the 2026-04-18 outage
- Corrupt-row fallback returns empty list, logs to stderr — correct
- **Cleared:** All test coverage in `test_memory.py` is comprehensive and tests the failure modes.

### `prompts.py`
Large, well-organised prompt file. Additions:
- `SALES_SYSTEM_PROMPT` and `MARKETING_SYSTEM_PROMPT` added as named constants
- `AGENT_SYSTEM_PROMPTS` dict maps agent_type → prompt
- `SKILL_LABELS` and `SKILL_PROMPTS` expanded with 8 skills: `qualify_lead`, `prepare_discovery`, `map_objections`, `sharpen_positioning`, `create_content`, `adapt_messaging`, `client_discovery_debrief`, `debrief_to_messaging`
- **Risk:** `test_prompt_sync.py` checks that `SKILL_LABELS` keys all exist in `SKILL_PROMPTS`. This is a good guard. But there is no reverse check — a skill could exist in `SKILL_PROMPTS` without a label. Low priority.
- **Risk:** The Sales prompt references `/Users/thomasthiadens/Ainstein` as a "secondary" source location. This local path is machine-specific and would silently fail on any other machine. If this is a production Slack bot, this reference should either be removed or made conditional.

### `slack_app.py`
Full Slack bot with socket mode. New additions:
- `_route_request()` — deterministic keyword router returning `(agent_type, skill)`. Clean and readable.
- File attachment support via `_download_files()` — correctly restricts bot token to known Slack hosts (`files.slack.com`, `slack-files.com`). Good security practice.
- 👎 reaction → feedback capture loop (`register_pending` / `pop_pending` / `append_feedback`) — correctly wired
- Per-thread locking (`_get_thread_lock`) — prevents race conditions on concurrent messages in the same thread
- Slash commands: `/analyse`, `/voorstel`, `/experts`, `/sales`, `/qualify`, `/discovery`, `/marketing`, `/content`, `/positioning`, `/objections`, `/debrief`, `/debrief-messaging`
- **Risk:** `_thread_locks` dict grows unbounded — a thread_ts is added on first use but never removed. In a high-volume environment this leaks memory. Low risk for current usage, but worth noting.
- **Risk:** `_MESSAGE_SKILLS` is capped at 500 with FIFO eviction — good. But `_thread_locks` has no equivalent cap.
- **Cleared:** SSL cert fix (`certifi`) at module top is correct and necessary for macOS python.org builds. Comment explains why it runs at import time.

### `requirements.txt`
Correct and complete. Pins `python-pptx>=1.0.0` (newly added), `pypdf>=4.0.0` (named import), `duckduckgo-search>=6.0.0`. All match tool usage in `tools.py`.

### `tests/` (3 files)
- `test_memory.py` — comprehensive, covers pydantic round-trip, schema version guard, corrupt-row fallback. Well written.
- `test_feedback.py` — covers pending state machine, markdown writing, multi-line blockquotes, folder creation. Complete.
- `test_prompt_sync.py` — structural sync check between `prompts.py` and `agents/*/system_prompt.md`. Good regression guard.
- **Flag:** No tests for `tools.py` (file reading, search scoring, caching). No tests for `slack_app.py` routing (`_route_request`). These are the highest-risk components that lack coverage.

---

## Content changes

### `01_Proposals/25_Nationale Nederlanden_Inkomen Collectief/` (new folder)
Several files added for an active Nationale Nederlanden opportunity:

- `26020_NN Inkomen Collectief_Aantekeningen.md/.pdf/.svg` — meeting notes from a call with Karin, Elif, Lotte on 20 April 2026. Notes are in RTF format (`.md` is actually RTF, not Markdown — file extension mismatch). Content: planning for a Blooming two-day event on 18–19 May. Notes are raw and short — useful as raw input but not structured for retrieval.
- `260402_NN Inkomen Collectief Fwd: Mogelijkheden voor 19 mei.pdf/.ps` — email forward about the 19 May options. Not read (PS file; PDF available).
- `26020_NN Inkomen Collectief_Transcript Call.docx` — call transcript, not read in detail.
- `NN Inkomen Collectief — Blooming Tweedaagse | Programmavoorstel 19 mei.gdoc` — Google Doc (proposal draft for 19 May). Not directly readable by `read_file` (gdoc format).
- `reMarkable Export vs. Transcriptie Vergelijking.gdoc` — comparison document, Google Doc format.
- `Divers/Document from my reMarkable: Inkomen Collectief.pdf` — reMarkable export.

**Completeness flags:**
- The `.md` file is actually RTF content — this will be read by `tools.py` as plain text and the RTF markup will appear as noise in search results. Consider converting to actual Markdown.
- `.gdoc` files are Google Doc stubs and will return `[Could not read file: ...]` from `_read_text`. The content lives in Google Drive and is not locally accessible by the bot. This is a known gap for gdoc-format files.
- No formal proposal document in a readable format yet — only raw notes and a gdoc draft.
- Key fields missing for retrieval: no explicit client summary, no structured challenge statement, no pricing information, no expert recommendations yet.

### `06_Marketing/presentations/Minkowski as a Label - reviewed.pptx`
Updated presentation. Content is extractable by `tools.py` via `python-pptx`. Not read in full. Flag: check that the "reviewed" version reflects the current Minkowski positioning (especially the "Nobody knows Minkowski" hook).

### `agents/` directory (7 files, all new)
Complete multi-agent architecture documentation:
- `agents/README.md` — architecture overview, routing diagram, slash command table, extension guide. Well written.
- `agents/orchestrator/routing.md` — full routing table and handoff patterns. Matches `slack_app.py` implementation. **Note:** routing table documents `/objections` and `/debrief` commands that are present in `slack_app.py` but not listed in `agents/README.md` slash command table — minor gap between the two reference files.
- `agents/sales/system_prompt.md` — human-readable reference. Correctly marked as reference (running version in `prompts.py`).
- `agents/marketing/system_prompt.md` — same. Both pass `test_prompt_sync.py` sentinel checks.
- `agents/sales/output_contracts.md`, `agents/sales/playbooks.md` — not read in full.
- `agents/marketing/output_contracts.md`, `agents/marketing/playbooks.md` — not read in full.
- `agents/sales/workflows/client_discovery_debrief.md`, `agents/marketing/workflows/debrief_to_messaging.md` — workflow docs.
- `agents/test_scenarios/` — 3 scenario files for manual testing. Good operational resource.

### `CLAUDE.md`
Updated system instructions for the Claude Code session. Reflects the three-skill structure and the source layer folder map. Consistent with current code.

### `README.md`
Not read. Likely project overview.

---

## Follow-ups

1. **Fix file extension mismatch (NN notes):** `26020_NN Inkomen Collectief_Aantekeningen.md` is RTF, not Markdown. The search layer will read RTF markup as noise. Convert to plain Markdown or rename to `.rtf`.
   — `01_Proposals/25_Nationale Nederlanden_Inkomen Collectief/26020_NN Inkomen Collectief_Aantekeningen.md`

2. **Add a structured proposal brief for the NN opportunity:** The current content is raw meeting notes. Add a one-page structured brief (client, challenge, program direction, open questions) so `search_files` can retrieve it meaningfully.
   — `01_Proposals/25_Nationale Nederlanden_Inkomen Collectief/`

3. **Fix potential message-history gap at max_iterations:** When `max_iterations` is hit, the fallback response is returned but the final assistant turn may not be appended to `messages` before `mem_save` is called in `slack_app.py`. Verify and fix if needed.
   — `agent.py` lines 99–107, `slack_app.py` line 243

4. **Remove duplicate `Path` import in `feedback.py`:** `from pathlib import Path` appears on line 4 and `from pathlib import Path as _Path` on line 20. Remove the alias version.
   — `feedback.py` lines 4 and 20

5. **Cap `_thread_locks` dict size in `slack_app.py`:** Like `_MESSAGE_SKILLS`, add a max-size eviction to prevent unbounded memory growth.
   — `slack_app.py` near line 120

6. **Add tests for `_route_request()` in `slack_app.py`:** The routing function is the entry point for all Slack interactions. It has no test coverage. Add parametrized tests covering the main keyword branches.

7. **Add tests for `tools.py` search scoring:** The `search_files` relevance scoring (name hits × 10 + content hits) and snippet extraction have no test coverage.

8. **Remove local fallback path `/Users/thomasthiadens/Ainstein` from Sales and Marketing system prompts:** Machine-specific path will silently fail on any deployment outside this machine.
   — `prompts.py` lines 268 and 343

9. **Sync slash command table in `agents/README.md`:** `/objections` and `/debrief` commands exist in `slack_app.py` but are missing from the slash command tables in `agents/README.md`.

---

## Cleared

- `memory.py` pydantic serialization fix is correct and well-tested — this was the root cause of the 2026-04-18 outage.
- `feedback.py` thread safety via `Lock` is correct.
- `slack_app.py` SSL cert fix is correctly placed at module top before `App()` construction.
- `_download_files()` bot-token scope restriction to known Slack hosts is a correct security control.
- `test_prompt_sync.py` sentinel checks all pass based on code review (no test run executed).
- `requirements.txt` is consistent with all imports in the codebase.
