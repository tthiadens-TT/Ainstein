# Review — 2026-04-28

**Window:** last 24h · **Files changed:** 2 (meaningful) · **Carry-forward open:** 5

---

## For the Minkowski team [commercial]

**New feedback document logged: `07_Feedback/260427_Feedback_Bronnen_en_Eigenaarschap.md`**
Jörgen corrected three errors in an AInstein-generated NN Inkomen Collectief status update:
- "Godfried" (named as external speaker Day 2) — came from an AI-generated summary, not the raw notes. Jörgen did not recognise the name.
- Minkowski facilitating Day 1 carousel rounds — incorrect. Minkowski has no Day 1 role.
- "Iets leuks & actiefs" as a Minkowski task — also incorrect; Day 1 is entirely client-owned.

The document correctly identifies the root cause: AI-generated summaries being treated as primary sources. The learnings are directly operational — label each programme element `[Klant]` / `[Minkowski]` / `[Nog te bepalen: wie?]`, trace everything back to raw files, and flag unconfirmed names explicitly rather than silently dropping or promoting them.

No content gaps in this document. It is well-structured and ready to use. No action required — logged correctly.

**Urgency escalation — NN Inkomen Collectief:** May 19 is now **21 days away**. Aline and Klazien remain unconfirmed (carry-forward items 6 and 7 below). This is now a blocking risk. If either facilitator is unavailable, replacement briefing time is running out.

---

## For the developer [dev]

**`routing.py` was modified today.** The current state of the file looks structurally sound. One observation:

- **Lines 63–64 — generic tokens in expert fallback.** The fallback `if any(w in t for w in ["expert", "facilitator", "who should", "team", "match"])` includes `"match"` and `"team"`, which are very common words. Example: "this matches our criteria" or "our team is aligned" — both would route to `(None, "match_experts")` if no marketing or sales words are also present. In practice this may be rare, but it is a latent mis-routing risk. Consider replacing `"match"` with `"match experts"` (full phrase) and `"team"` with something more specific, or add a minimum-confidence guard. Tagged `[dev]` — no urgent fix needed, but worth noting before the routing layer grows further.

No logic errors, security issues, or missing tests observed. Test cache confirms a full suite is in place across routing, feedback, memory, prompt sync, and tools.

---

## New follow-ups

1. **[dev] Tighten generic tokens in expert fallback (`routing.py:63`)** — `"match"` and `"team"` may cause unexpected `match_experts` routing for unrelated messages. Consider using `"match experts"` and dropping `"team"`, or adding phrase-level guards. Low urgency; flag before next routing expansion.

---

## Carry-forward (still open)

1. **[commercial] [NN Inkomen Collectief] Fix year typo in LEGO summary** — file states "Date: May 19, 2025", should be 2026. Correct before sharing with Pierre or client. File: `01_Proposals/25_Nationale Nederlanden_Inkomen Collectief/LEGO Serious Play workshop/AI summary van call LEGO man voor Inkomen Collectiet (IC) sessie….md`. First flagged: 2026-04-25.

2. **[commercial] Rename `EDIT_Einstein_pitch.html` → `EDIT_AInstein_pitch.html`** — incorrect product name in filename. PDF and PNG already correctly named. First flagged: 2026-04-25.

4. **[commercial] WheelOfReasoning.md — facilitation content gaps** — document is well-structured but explicitly missing: facilitation script, worksheet/canvas format, typical participant outputs, required facilitator level, known failure modes. Low urgency unless a facilitator without prior experience needs to run this. First flagged: 2026-04-25.

6. **[commercial] [NN Inkomen Collectief] Confirm Aline and Klazien for May 18–19** — Pierre is confirmed for May 19 (LEGO session). Aline and Klazien remain unconfirmed with 21 days to event. **Urgency: high.** First flagged: 2026-04-22.

7. **[commercial] [NN Inkomen Collectief] Get budget indication before finalising scope** — no price anchor present in source layer. Requires human contact with NN. First flagged: 2026-04-22.

---

## Cleared

3. **[dev] Dead code in `routing.py:63–64`** — previous review flagged this as unreachable. Current code shows lines 63–64 contain a `match_experts` fallback for messages with expert words but no marketing or sales context (e.g. "who should facilitate our strategy session?"). This is reachable and correct. The original dead-code concern was either about a prior version of the file or a misread. Clearing this item. The separate new follow-up (item 1 above) flags the weaker token choices in those same lines, which is a different and lower-severity concern. First flagged: 2026-04-25.
