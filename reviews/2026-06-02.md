# Review — 2026-06-02

**Window:** last 24h · **Commits:** 9 (incl. 3 pptx_builder brand fixes na eerste run) · **PRs merged:** n/a (GitHub MCP auth failed) · **Drive files changed:** Drive: SSH onbereikbaar · **Skills checked:** 15 · **Carry-forward open:** 3

> **Drive:** `ssh thomas@35.253.206.86` returned `Permission denied (publickey)`. Drive-wijzigingen zijn onbekend — dit is géén bevestiging dat er niets is veranderd.
> **GitHub MCP:** auth failed (OAuth, verwacht in headless runs). PR/issue-info niet beschikbaar.

---

## For the Minkowski team [commercial]

Drive onbereikbaar via SSH — kon bronnenlaag niet controleren. Geen commerciële bevindingen mogelijk voor deze run.

**⚠️ Test de PPTX-uitvoer vóór de volgende klantverstrekking.** De brandkleuren en fonts zijn vandaag 3× bijgesteld op basis van `MK-new-brandbook.pptx` (Charlotte). De finale staat is theoretisch correct, maar er is geen visuele test gemaakt. Vraag Ainstein een proefvoorstel te genereren en controleer cover, content-slides en back-slide. Let specifiek op het Minkowski-wordmark: dat gebruikt `Sen ExtraBold`, een font dat op client-machines vrijwel zeker niet aanwezig is (zie [dev-5] hieronder).

---

## For the developer [dev]

### `34a3d9b` — export_proposal_deck: Drive upload vóór return

De tool sloeg de PPTX tot nu toe alleen op in een temp-file voor de Slack-queue; de agent had geen Drive-link om te delen en de upload kon stil mislukken. Nu wordt het bestand tijdens de tool-executie al naar `00_Werkdocumenten` geüpload, en het resultaat bevat een `url`-veld met de `webViewLink`.

**[dev-3] Tool description zegt "Always share the 'url'" maar url is conditioneel — `tools.py:2159`**

```python
if drive_url:
    result["url"] = drive_url
```

Als de Drive-upload mislukt (exception gevangen op regel ~2153), ontbreekt `url` in het resultaat. De tool-description zegt `"Always share the 'url' from the result with the user."` — die formulering klopt niet als `url` er niet is. De agent kan dan een missing-key melding geven of inconsistent gedragen. Fix: verander de description naar `"Share the 'url' from the result if present."` of voeg een fallback lege string toe.

**Verder:** de Drive-lookup valt netjes terug op een live API-call als `_get_drive_folder_ids()` niets teruggeeft. Exception-handling en logging zijn aanwezig. Goed gedaan.

---

### `60c704b` — slack: failure notification bij dubbele fallback-fout

Nieuwe functie `_notify_failure` in `slack_app.py`. Stuur een gerichte DM naar `AINSTEIN_STATUS_CHANNEL` als zowel de hoofdagent als de Anthropic-fallback faalt. Foutclassificatie (credits, API-key, rate limit, overig) met concrete acties voor Thomas.

**[dev-4] Rate-limit bericht zegt "probeert automatisch opnieuw" maar retry is hier al opgegeven — `slack_app.py:972`**

```python
elif "rate limit" in err_str:
    actie = "Even wachten — Ainstein probeert automatisch opnieuw."
```

`_notify_failure` wordt alleen aangeroepen ná de dubbele fallback, dus op dit punt is er géén automatische retry meer — de request is gedropt. De tekst kan Thomas onnodig laten wachten. Fix: `"Ainstein kon dit verzoek niet verwerken. Stuur het opnieuw als de rate limit is afgelopen."` of vergelijkbaar.

---

### `04c3802` — brain.md: activiteitsvragen → Drive-lookups

Correcte fix. Wanneer gebruikers vragen "wat heb je vandaag gedaan?" of "geef een update van vandaag", gebruikt Ainstein nu `list_recent_files` in plaats van te reflecteren op zijn eigen AI-identiteit. Geen issues.

---

### `b1d31b1` + `1430b4f` + `e89e983` — pptx_builder: drie brand-iteraties in 21 minuten

Drie opeenvolgende commits (09:20, 09:32, 09:41) op `pptx_builder.py`. Aanleiding: Charlotte uploadde `MK-new-brandbook.pptx` naar Drive. De commits schetsen een iteratief zoekproces:

- **`b1d31b1`** (09:20): first pass op basis van "reverse-engineered Minkowski decks" — Work Sans + gold (#FFE433)
- **`1430b4f`** (09:32): overridden door brandbook-inzicht — Helvetica Neue + teal (#98D2CF) + Play voor titels
- **`e89e983`** (09:41): definitieve correctie na lezen van slide-XML — font scale 14/25/50pt, Sen ExtraBold wordmark, alle achtergronden wit

De finale staat (`e89e983`) is de authoritatieve versie. Twee issues:

**[dev-5] `Sen ExtraBold` is geen systeemfont — font-substitutie op Windows — `pptx_builder.py:55,208,326`**

```python
"font_wordmark": "Sen ExtraBold",  # Minkowski wordmark
```

`Sen` is een Google Font en is niet geïnstalleerd op standaard Windows- of macOS-systemen. Wanneer een client de PPTX opent, ziet PowerPoint `Sen ExtraBold` niet en valt terug op Arial. Hetzelfde geldt voor `Helvetica Neue` op Windows (niet standaard aanwezig; valt terug op Arial). Het wordmark op cover en back-slide wordt dan anders weergegeven dan bedoeld.

Aanbevelingen (kies één):
  a. Vervang `Sen ExtraBold` door een veilig alternatief dat overal aanwezig is (bijv. `Georgia` of `Calibri Bold`) voor productiedekken.
  b. Embedded fonts via `pptx` zijn niet native ondersteund in python-pptx — overweeg het output-bestand handmatig te openen en "Embed fonts in file" aan te zetten voor definitieve klantdekken.
  c. Documenteer dit als bekende beperking: "Fonts kloppen alleen als Sen en Helvetica Neue geïnstalleerd zijn."

**[dev-6] `dark`-parameter in `_add_content_slides` is dode code — `pptx_builder.py:66,107`**

```python
DARK_SECTIONS = {"commercial notes", "commerciële notities", "risks / weak spots", "risico's"}
# regel 107:
_add_content_slides(prs, sec_name, sec_body, dark=dark)
```

In `e89e983` is de dark-branch logica (`bg_color`, `text_color`, `heading_color`) verwijderd, maar de functie-signature behoudt `dark: bool = False` en `DARK_SECTIONS` activeert nog steeds `dark=True` voor "commercial notes" en "risks"-secties. Die waarde wordt nu stilzwijgend genegeerd — alle slides zijn wit ongeacht het type. Dit is waarschijnlijk bewust (nieuw brandbook gebruikt geen donkere slides), maar het is nu misleidende dode code. Fix: verwijder `dark`-parameter, `DARK_SECTIONS`-constant, en de `dark=dark`-aanroep.

---

### `76b24c9` — list_recent_files: limit-parameter + RFC3339-fix

Lost beide issues op die in de 2026-05-31 review zijn gevlagd ([dev-1] en [dev-2]):
- Timestamp krijgt `Z`-suffix: RFC 3339-compliant.
- `limit`-parameter (default 50) voorkomt context-overflow bij brede tijdvensters.

✅ Beide carry-forward items gecleared.

---

### `c1f1186` — scripts/list_drive_changes.py

Nieuw script voor Drive-monitoring in dagelijkse review. Wrapt `tools.list_recent_files(hours=N)` en print JSON. Laadt `.env` vóór de import van `tools.py` — correct. Geen `--limit` CLI-argument (gebruikt altijd default 50) — voor 26u dagelijks gebruik voldoende, geen actie nodig.

---

## GitHub

GitHub MCP auth failed — zelfde als 2026-05-31. PR/issue-lijst niet opgehaald. Op basis van lokale git: geen open branches zichtbaar.

---

## Skills

Geen skill-bestanden gewijzigd in de afgelopen 24u. 15 bestanden ongewijzigd. Geen inconsistenties gedetecteerd.

---

## New follow-ups

3. **[dev]** `tools.py` export_proposal_deck — tool description zegt "Always share the 'url'" maar `url` is conditioneel (alleen aanwezig als Drive-upload slaagt). Verander naar "Share the 'url' if present." `tools.py:1957–1975` (schema description).
4. **[dev]** `slack_app.py:972` — rate-limit actiebericht zegt "probeert automatisch opnieuw" maar op dit punt is er geen retry meer. Aanpassen naar feitelijke tekst.
5. **[dev]** `pptx_builder.py:55,208,326` — `Sen ExtraBold` is geen systeemfont; op Windows/macOS zonder installatie → Arial-fallback. Kies: veilig font, embed-instructie documenteren, of beperking markeren. Prioriteit vóór eerstvolgende klantverstrekking.
6. **[dev]** `pptx_builder.py:66,107,263` — `dark`-parameter en `DARK_SECTIONS` zijn dode code na `e89e983`. Verwijder om misleiding te voorkomen. Lage prioriteit, clean-up.

---

## Carry-forward (still open)

- **[dev]** `feedback.py:171` — `fresh` detection is dead code in Drive mode (`gaps_file` nooit aanwezig lokaal; Drive mode slaat dit pad over). Misleidend, geen runtime bug. Eerst gevlagd: 2026-05-28.
- **[docs]** `DEPLOYMENT.md` — channel-scoped memory (`mem_key = thread_ts or channel`) niet gedocumenteerd als bekende beperking. Twee gelijktijdige gebruikers in hetzelfde channel kunnen elkaars conversatiecontext overschrijven. Eerst gevlagd: 2026-05-28.
- **[infra]** GitHub MCP niet beschikbaar in scheduled runs (OAuth-gebaseerd, verwacht gedrag). Review-taak valt al terug op `git log`. Geen actie nodig. Eerst gevlagd: 2026-05-31.

---

## Cleared

- ✅ **[dev-1]** `tools.py:1553` — RFC3339 timestamp ontbrak `Z`-suffix. Opgelost in `76b24c9`.
- ✅ **[dev-2]** `tools.py:1547` — Geen paginering-limiet in `list_recent_files`. Opgelost in `76b24c9` (`limit`-parameter toegevoegd).
