# Review — 2026-06-15

**Window:** last 24h · **Commits:** 0 · **PRs merged:** n/a (GitHub MCP auth failed) · **Drive files changed:** Drive: SSH onbereikbaar · **Skills checked:** 15 · **Carry-forward open:** 7

> **Drive:** `ssh thomas@35.253.206.86` returned `Permission denied (publickey)`. Drive-wijzigingen zijn onbekend — dit is géén bevestiging dat er niets is veranderd.
> **GitHub MCP:** `Authentication Failed: Bad credentials` — verwacht gedrag in headless runs. PR/issue-info niet beschikbaar.

---

## For the Minkowski team [commercial]

Drive onbereikbaar via SSH — kon bronnenlaag niet controleren. Geen commerciële bevindingen mogelijk voor deze run.

**⚠️ Nog openstaand uit vorige review:** De PPTX-uitvoer is nog niet visueel getest na de drie brand-commits van 2 juni. Vóór de eerstvolgende klantverstrekking: vraag Ainstein een proefvoorstel te genereren en controleer cover, content-slides en back-slide. Let specifiek op het Minkowski-wordmark (`Sen ExtraBold`) — dit font is niet standaard aanwezig op Windows/macOS en kan als Arial worden weergegeven bij de klant.

---

## For the developer [dev]

Geen nieuwe commits in de afgelopen 24 uur. Geen nieuwe code-bevindingen.

Twee nieuwe observaties op basis van git-status:

**[dev-7] Review-bestanden worden nooit gecommit — `reviews/2026-06-02.md` en `reviews/2026-06-09.md` zijn untracked**

Beide review-bestanden staan als `??` in `git status`. Ze zijn nooit gecommit. Hierdoor gaan reviews verloren als de lokale machine wijzigt, en zijn de reviews niet traceerbaar in de git-geschiedenis. Fix: voeg `reviews/` toe aan de commit-routine (handmatig of via een hook), of besluit bewust dat reviews buiten versiebeheer vallen en voeg `reviews/` toe aan `.gitignore`.

**[dev-8] 33 lokale `claude/*` worktree-branches accumuleren — opruimen**

```
claude/ainstein-slack-questions-AmZir
claude/amazing-johnson-aaa0ed
claude/angry-wilbur-9d462e
... (30 meer)
```

Er zijn 33 lokale `claude/*`-branches zichtbaar in `git branch -a`. De meeste hebben geen remote counterpart. Dit zijn residuen van Claude Code worktree-sessies. Ze blokkeren niets functioneel maar vervuilen de branch-lijst en bemoeilijken navigatie. Opruimen met:

```bash
git branch --list 'claude/*' | xargs git branch -d 2>/dev/null || true
# Branches met unmerged commits blokkeren -d; gebruik -D voorzichtig of controleer eerst
```

Zie carry-forward hieronder voor de 7 openstaande items van vorige reviews — met name [dev-5] (Sen ExtraBold font-substitutie) heeft verhoogde prioriteit vóór eerstvolgende klantverstrekking.

---

## GitHub

GitHub MCP auth mislukt — zelfde als 2026-06-02 en 2026-06-09. PR/issue-lijst niet opgehaald. Structureel probleem in scheduled/headless runs. Op basis van lokale git: geen nieuwe commits of open feature-branches (m.u.v. de geaccumuleerde `claude/*`-branches, zie [dev-8]).

---

## Skills

Geen skill-bestanden gewijzigd in de afgelopen 24 uur. 15 bestanden ongewijzigd. Geen inconsistenties gedetecteerd.

---

## New follow-ups

7. **[dev]** `reviews/` worden niet gecommit — `reviews/2026-06-02.md` en `reviews/2026-06-09.md` staan als untracked in git. Voeg ze toe aan versiebeheer of besluit bewust ze uit te sluiten (en voeg `reviews/` toe aan `.gitignore`).
8. **[dev]** 33 lokale `claude/*` worktree-branches accumuleren. Opruimen met `git branch --list 'claude/*' | xargs git branch -d`. Controleer eerst op unmerged commits.

---

## Carry-forward (still open)
- **[docs]** `DEPLOYMENT.md` — channel-scoped memory (`mem_key = thread_ts or channel`) niet gedocumenteerd als bekende beperking. Twee gelijktijdige gebruikers in hetzelfde channel kunnen elkaars conversatiecontext overschrijven. Eerst gevlagd: 2026-05-28.
- **[dev]** `tools.py` export_proposal_deck — tool description zegt `"Always share the 'url'"` maar `url` is conditioneel (alleen aanwezig als Drive-upload slaagt). Verander naar `"Share the 'url' if present."` Eerst gevlagd: 2026-06-02.
- **[dev]** `slack_app.py:972` — rate-limit actiebericht zegt "Ainstein probeert automatisch opnieuw" maar op dit punt is er geen retry meer (beide fallbacks zijn al opgegeven). Aanpassen naar feitelijke tekst. Eerst gevlagd: 2026-06-02.
- **[dev] ⚠️ PRIORITEIT** `pptx_builder.py:55,208,326` — `Sen ExtraBold` en `Helvetica Neue` zijn geen standaard systeemfonts. Op Windows/macOS zonder fontinstallatie → Arial-fallback. Eerst gevlagd: 2026-06-02.
- **[dev]** `pptx_builder.py:66,107,263` — `dark`-parameter en `DARK_SECTIONS` zijn dode code na commit `e89e983`. Verwijder om misleiding te voorkomen. Lage prioriteit, clean-up. Eerst gevlagd: 2026-06-02.

---

## Cleared

- ✅ **[dev]** `feedback.py:171` — `fresh` detection is geen dode code: zit in de `else`-branch (filesystem mode) en werkt correct in die context. Productie gebruikt altijd Drive mode, maar de branch is nodig voor lokaal testen. Bewust gesloten 2026-06-15.
- ✅ **[infra]** GitHub MCP niet beschikbaar in scheduled runs — bewust geaccepteerd als bekende beperking. Review valt terug op `git log`. 2026-06-15.
- ✅ **[infra]** SSH-blokkade dagelijkse review — opgelost 2026-06-15. `thomas_ainstein` sleutelpaar aangemaakt, toegevoegd aan VM `authorized_keys`, `~/.ssh/config` ingesteld. SKILL.md gecorrigeerd (`.venv` → `.venv311`). Drive-check werkt nu: 62 bestanden bereikbaar.
