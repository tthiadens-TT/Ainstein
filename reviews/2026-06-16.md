# Review — 2026-06-16

**Window:** last 24h · **Commits:** 3 (2 distinct changesets) · **PRs merged:** n/a (GitHub MCP auth failed — known limitation) · **Drive files changed:** 0 · **Skills checked:** 16 (0 gewijzigd) · **Carry-forward open:** 2

---

## For the Minkowski team [commercial]

**Drive:** geen wijzigingen in de bronnenlaag in de afgelopen 26 uur (62 bestanden ongewijzigd via VM-check).

**Goed nieuws voor klantverstrekking:** Ainstein-gegenereerde PPTX-bestanden bevatten nu het Sen ExtraBold-font ingebakken via de OOXML-standaard. Dat betekent dat de Minkowski-huisstijl correct wordt weergegeven op de machine van de klant, zelfs als het font daar niet geïnstalleerd is. Voorheen werd dit vervangen door Arial. Aanbeveling: laat één proefvoorstel genereren en open het op een Windows-machine of een Mac zonder Sen-installatie ter verificatie, vóór de eerstvolgende klantverstrekking.

---

## For the developer [dev]

### 9dcb699 — fix: carry-forward items + Sen ExtraBold font embedding

Vijf openstaande carry-forward items zijn in één commit opgelost:

- `DEPLOYMENT.md`: channel-scoped memory als bekende beperking gedocumenteerd ✅
- `tools.py`: `export_proposal_deck`-beschrijving gecorrigeerd (`url` conditioneel) ✅
- `slack_app.py`: rate-limit actiebericht herschreven — geen valse retry-belofte meer ✅
- `pptx_builder.py`: `DARK_SECTIONS` / `dark`-parameter verwijderd (dode code) ✅
- `pptx_builder.py`: Sen ExtraBold ingebakken via OOXML §22.4.2.3 (`_embed_sen_extrabold`) ✅

**Nieuwe observaties op de font-embedding code:**

1. **`pptx_builder.py:~110`** — `rid = "rIdSenEB"` is hardcoded. Als in de toekomst een tweede font wordt ingesloten, collideren relationship-IDs in `presentation.xml.rels`. Momenteel geen probleem (één font), maar de constante maakt het onveilig uitbreiden. Lage prioriteit.

2. **`pptx_builder.py:_embed_sen_extrabold`** — De functie loopt stilzwijgend door als `_FONT_ASSET` niet bestaat (retourneert ongewijzigde bytes). Goed failsafe. Geen actie nodig, alleen bewustzijn.

### 7154a08 — feat(web_search): Tavily met DDGS-fallback

Code is schoon: env-key-gated routing, exception-safe fallback. Geen regrissierisico.

**Openstaand aandachtspunt:**

3. **`tools.py:_web_search_tavily`** — `search_depth="advanced"` is hardcoded. Advanced searches kosten meer Tavily API-credits dan `"basic"`. Op de gratis tier (1.000 searches/maand) kan dit het quotum sneller uitputten bij intensief gebruik. Overweeg de depth configureerbaar te maken via env var (`TAVILY_SEARCH_DEPTH`, default `"basic"`), of documenteer de implicatie in `.env.example`.

4. **`[infra]`** — `TAVILY_API_KEY` staat nog niet in `.env` op de VM (de `.env.example` heeft hem als commentaar). VM valt terug op DDGS. Activeer de key op de VM om rijkere zoekresultaten te krijgen.

---

## GitHub

GitHub MCP auth mislukt — zelfde gedrag als in vorige scheduled runs. PR/issue-lijst niet opgehaald. Op basis van git log: twee commits gemerged in `main` in de afgelopen 24 uur (zie boven). Commit `b215394` heeft dezelfde inhoud als `9dcb699` — vermoedelijk een residuele branch-commit die al in `main` zit.

---

## Skills

Geen skill-bestanden gewijzigd. 16 bestanden ongewijzigd. De nieuwe Tavily-functionaliteit is beschikbaar voor alle skills die `web_search` aanroepen — geen skill-aanpassing nodig.

---

## New follow-ups

1. **[commercial]** Visueel testen van PPTX-font-embedding vóór eerstvolgende klantverstrekking. Open een Ainstein-gegenereerd deck op Windows of een Mac zonder Sen-installatie en controleer of Sen ExtraBold correct wordt weergegeven. (`pptx_builder.py:_embed_sen_extrabold`)
2. **[dev]** `pptx_builder.py` — hardcoded `rid = "rIdSenEB"`. Niet urgent, maar maakt toekomstige uitbreiding met een tweede font onveilig. Overweeg dynamisch te genereren bij uitbreiding.
3. **[dev]** `tools.py:_web_search_tavily` — `search_depth="advanced"` hardcoded. Overweeg env-var `TAVILY_SEARCH_DEPTH` (default `"basic"`) om quotumgebruik te beheersen.
4. **[infra]** Zet `TAVILY_API_KEY` in `.env` op de VM om Tavily te activeren. DDGS werkt als fallback maar is minder betrouwbaar.

---

## Carry-forward (still open)

- **[dev]** `reviews/` worden nooit gecommit — `reviews/2026-06-02.md`, `reviews/2026-06-09.md`, `reviews/2026-06-15.md` staan als untracked in git. Voeg ze toe aan versiebeheer of voeg `reviews/` bewust toe aan `.gitignore`. Eerst gevlagd: 2026-06-15.
- **[dev]** 34 lokale `claude/*` worktree-branches accumuleren (gisteren 33, vandaag 34). Opruimen met `git branch --list 'claude/*' | xargs git branch -d`. Controleer eerst op unmerged commits. Eerst gevlagd: 2026-06-15.

---

## Cleared

- ✅ **[docs]** `DEPLOYMENT.md` channel-scoped memory — gedocumenteerd in 9dcb699. Gesloten 2026-06-16.
- ✅ **[dev]** `tools.py` export_proposal_deck url-beschrijving — gecorrigeerd in 9dcb699. Gesloten 2026-06-16.
- ✅ **[dev]** `slack_app.py:972` rate-limit actiebericht — gecorrigeerd in 9dcb699. Gesloten 2026-06-16.
- ✅ **[dev] PRIORITEIT** `pptx_builder.py` Sen ExtraBold font-substitutie — opgelost via OOXML-embedding in 9dcb699. Gesloten 2026-06-16.
- ✅ **[dev]** `pptx_builder.py` dode `dark`-code en `DARK_SECTIONS` — verwijderd in 9dcb699. Gesloten 2026-06-16.
